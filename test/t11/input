{{! Test variables. -}}
{{ #section brewery ;
     separator = "\n\n",
     brewery_name = name,
     beer_names = beer[!season]/beer_name,
     seasonal_beers = beer[season] }}
{{ $brewery_name }} brewery:
  Year Round Beers:  {{ $beer_names ; separator=", " }}
{{   #if $seasonal_beers }}
  Seasonal Beers:  {{
        #section $seasonal_beers ; separator=", " }}
{{ beer_name }} ({{ season }})
{{      #end }}
{{  #end }}
{{ #end }}
